rules_version = '2';

// Cloud Storage for Firebase のセキュリティルール
// このプロジェクトでは Storage を使用していないため、すべてのアクセスを拒否
service firebase.storage {
  match /b/{bucket}/o {
    // すべての読み取り・書き込みを拒否
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
